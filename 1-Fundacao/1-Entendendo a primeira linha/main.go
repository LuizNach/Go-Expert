package main

/**
Primeira lição:
- Todo arquivo deve referencia um pacote em seu inicio
- Todo nome de pacote deve ser do mesmo nome do folder/directory que contem os arquivos go
- Único arquivo que não possui o nome do pacote como nome do directory/folder é o main.go
- Quando há mais de um arquivo .go no mesmo folder/directry, todo devem ter o mesmo nome de pacote
- Quando há dois arquivos .go no mesmo folder com mesmo package name, quer dizer que tudo que há em um arquivo há no outro também se rodarmos "go run *"
*/

func main() {
	println(a)
}
